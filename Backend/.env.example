PORT=5000
# MongoDB Atlas: get your connection string from Atlas → Cluster → Connect → Connect your application
# Replace <username>, <password>, and <cluster> with your values. Use a DB user (not your Atlas login).
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/rentease?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-change-in-production
